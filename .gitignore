# Visual Studio
.vs/
x64/
*.user
*.suo
*.VC.db
*.VC.opendb
Project/x64/

# Runtime folders (not tracked — user populates these)
tools/
Clips/
/Output/
data/logs/

# KO scan temp frames (cache .cache files ARE tracked)
data/cache/temp/

# Ground truth bulk frames — only labelled frames (frame_NNNNNN_*.png) are tracked
examples/ground_truth/*/frames/frame_??????.png
.claude/settings.local.json
