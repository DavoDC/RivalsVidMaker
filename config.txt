# CompilationVidMaker Configuration (Python pipeline)

# Root folder containing character subfolders (e.g. THOR\, SQUIRREL_GIRL\)
ClipsPath=C:\Users\David\Videos\MarvelRivals\Highlights

# Output folder for encoded MP4s and description files
OutputPath=C:\Users\David\Videos\MarvelRivals\Output

# Folder containing ffmpeg.exe and ffprobe.exe
FFMPEGPath=tools

# Path to Tesseract OCR binary (used by KO detection)
TesseractPath=C:\Program Files\Tesseract-OCR\tesseract.exe

# Cache folder for KO detection results (per-clip .ko.json files)
CacheDir=data\cache

# Minimum batch duration in seconds — batches shorter than this are skipped
# (default: 600 = 10 minutes)
MinBatchSeconds=600

# Target batch duration in seconds — aim for this per compilation video
# (default: 900 = 15 minutes)
TargetBatchSeconds=900
