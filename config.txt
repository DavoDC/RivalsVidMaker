# CompilationVidMaker Configuration

# Root folder containing character subfolders (e.g. THOR\, DR_S\)
ClipsPath=C:\Users\David\Videos\MarvelRivals\Highlights

# Output folder for encoded MP4s and description files
OutputPath=C:\Users\David\Videos\MarvelRivals\Output

# Folder containing ffmpeg.exe and ffprobe.exe
FFMPEGPath=FFMPEG\

# Minimum batch duration in seconds to bother encoding (default: 600 = 10 minutes)
# Batches shorter than this are skipped — not worth uploading to YouTube
MinBatchSeconds=600
